namespace Netfox.Core.Enums
{
    /// <summary> UP - from client to server DOWN - from server to client.</summary>
    public enum DaRFlowDirection
    {
        /// <summary> An enum constant representing the up option.</summary>
        up,

        /// <summary> An enum constant representing the down option.</summary>
        down,

        /// <summary> An enum constant representing the non option.</summary>
        non
    }
}