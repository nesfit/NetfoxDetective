namespace Netfox.Core.Enums
{
    /// <summary> Values that represent ExportState.</summary>
    public enum ExportState
    {
        /// <summary> An enum constant representing the valid whole option.</summary>
        ValidWhole,

        /// <summary> An enum constant representing the valid fragment option.</summary>
        ValidFragment,

        /// <summary> An enum constant representing the malformed option.</summary>
        Malformed
    }
}